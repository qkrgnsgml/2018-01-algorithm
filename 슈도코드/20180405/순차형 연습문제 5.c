#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <math.h>

void main()
{
	printf("2014440892 이건우 \n");
	int x = 40, y = 38, oil, time;
	float z;
	double ab = sqrt(garo * garo + sero * sero);
	oil = z / 5;
	time = z / 25;
	printf("이동하는데에 필요한 연료 : %d \n걸린시간 : %d\n", oil, time);
}